import { redirect } from "next/navigation";

// The /chat route itself just renders the parallel routes layout (sidebar + default chat view)
// This file is required by Next.js for the parallel route layout to work
export default function ChatPage() {
  return null;
}
